server {
    listen 80;

    location / {
        root /usr/share/nginx/html;
        index index.html;
    }

    location /octobot-api/ {
        proxy_pass http://snipbot.railway.internal:8000/;
        proxy_set_header Host $host;
        add_header Access-Control-Allow-Origin *;
    }
}
